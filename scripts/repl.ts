// I use this directory to test out things using my projects node_modules or to run scripts needed for
// Migration, provisioning etc.
const main = async () => {};

// Kick off the async function
main().then(_ => {
  // console.log(results);
});
